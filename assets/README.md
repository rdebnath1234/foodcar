# Assets Guide

This folder stores static files such as JSON seed data and image/logo assets.

## Future Implementations

- Move static JSON to versioned seed files with schema checks.
- Optimize image assets and generate multiple resolutions where needed.
- Add validation scripts to ensure asset references are valid in `pubspec.yaml`.
- Introduce environment-specific asset sets (dev/staging/prod).
- Add script to sync seed assets from backend snapshots.
